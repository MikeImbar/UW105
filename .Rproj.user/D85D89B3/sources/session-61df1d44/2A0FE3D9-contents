x = sort(rexp(100))

range01 <- function(x){
  (x-min(x))/(max(x)-min(x))
}

range01(sorted_distances)
sorted_distances

transparency_range <- function(x){   (x - min(x))/(max(x)-min(x)) * (0.3 - 1) + newMin }

(x-min(x))
